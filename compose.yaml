include:
  - services/caddy/compose.yaml
  # Authelia is kept in the repository but disabled because qBittorrent and
  # Filebrowser use their own authentication.
  # - services/authelia/compose.yaml
  - services/qbittorrent/compose.yaml
  - services/filebrowser/compose.yaml
  - services/jellyfin/compose.yaml

networks:
  proxy:
    name: homelab_proxy
    external: true
